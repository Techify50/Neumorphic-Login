import 'package:flutter/cupertino.dart';

class Pink_Height extends StatelessWidget {
  double pink_height = 30;
  Pink_Height({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: pink_height,
    );
  }
}
